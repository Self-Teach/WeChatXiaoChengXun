/**
 * pages/profile/address/address.wxss
 * 功能：控制地址列表卡片的间距、排版和按钮样式，与设计令牌保持一致。
 * 用法：在 address.wxml 中组合 .card/.btn 等工具类；此处仅补充页面专属布局。
 * 尺寸（可调）：卡片间距 var(--spacing-24)、按钮高度 64rpx，按需调整。
 * 背景/配色：标签 badge 使用主题色；背景为 page-container 默认浅色。
 * 位置/布局：整页使用 column 布局；按钮宽度 100%。
 * 交互：按钮采用 btn 类，focus/active 状态由 util.wxss 控制。
 * 依赖/风险：确保 app.wxss 已引入 util 类；若新增滑动删除请注意与 card 阴影的冲突。
 */
.address-page { display: flex; flex-direction: column; }
.address-item { border: 1rpx solid var(--color-line); border-radius: var(--radius-lg); background: #fff; }
.address-header { align-items: center; gap: var(--spacing-12); }
.actions button { flex: 1; }
.add-btn { width: 100%; }
